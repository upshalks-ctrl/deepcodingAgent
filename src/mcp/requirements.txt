# MCP 搜索服务器依赖

# 必需依赖
aiohttp>=3.8.0
requests>=2.28.0

# 可选依赖 - 搜索引擎
ddgs>=5.0.0  # DuckDuckGo 搜索 (原 duckduckgo-search 已重命名)
arxiv>=1.4.0
wikipedia>=1.4.0

# MCP 协议支持 (可选)
# mcp>=1.0.0  # 如果使用 MCP 客户端库
